public class EmailService {
    public void sendEmail(String emailAddress, String message){
        System.out.println("success send email notification to " + emailAddress + " : " + message);
    }
}
